import React from 'react';
import c from './Login.module.css';

const Login = (props) => {
    return(
        <div className={c.loginPage}>
           Login
        </div>
    )
};

export default Login;